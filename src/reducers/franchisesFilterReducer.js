const initialState = ['Проворный ткачик', 'ПРОФИ-СПОРТ', 'Проспект', 'Просто Чудо', 'Прокат Пони+', 'Subway'];

export const franchisesFilterReducer = (state = initialState, action) => {
    switch (action.type) {
        default :
            return state
    }
};